[project]
name = "mydiet"
version = "0.1.0"
description = "Personal diet dashboard with Firestore storage and Gemini-powered diary analysis."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.0.0",
  "google-cloud-firestore>=2.21.0",
  "google-genai>=1.0.0",
  "pydantic>=2.8.0",
  "pydantic-settings>=2.4.0",
  "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.0",
  "ruff>=0.6.0",
]

[project.scripts]
mydiet = "mydiet.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mydiet"]

[tool.ruff]
line-length = 100
target-version = "py311"
